version: '3'
services:
    app:
        image: manzolo/gestionespesefamiliari_full_app
        restart: always
        build: 
          context: .
          dockerfile: .docker/full/php/Dockerfile
        container_name: gestionespesefamiliari_full_app
        volumes:
            - ./app/cache:/app/app/cache
            - ./app/logs:/app/app/logs
            - ./app/config/parameters.yml:/app/app/config/parameters.yml
        ports:
            - ${HTTP_PORT}:8030
