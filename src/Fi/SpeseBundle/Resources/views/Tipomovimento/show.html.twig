{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tipomovimento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ entity.tipo }}</td>
            </tr>
            <tr>
                <th>Abbreviazione</th>
                <td>{{ entity.abbreviazione }}</td>
            </tr>
            <tr>
                <th>Segno</th>
                <td>{{ entity.segno }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipomovimento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipomovimento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
